#!/usr/bin/env python3
# -*- coding: utf-8 -*-

-------------------------------------------------------------

# 1.1

# 1.1.1 Input and output
name = input('Please input your name:')
print('hello,', name)

# 1.1.2 Data Type
0xff
10_000_000_000, 0xa1b2_c3d4
1.23e9, 1.2e-5    # 浮点数运算则可能会有四舍五入的误差
# 字符串:允许用r''表示''内部的字符串默认不转义
        允许用'''...'''的格式表示多行内容
none

# 1.1.3 Variable
a = 'ABC'
b = a
a = 'XYZ'
print(b)
>>> ABC
# 对变量赋值x = y是把变量x指向真正的对象，该对象是变量y所指向的。随后对变量y的赋值不影响变量x的指向。

-------------------------------------------------------------

# 1.2 String

# 1.2.1
len()

#1.2.2
print('Hi, %s, you have $%d.' %('Jack',10000))
占位符	替换内容
%d	整数
%f	浮点数
%s	字符串
%x	十六进制整数

-------------------------------------------------------------

# 1.3 list and tuple

# 1.3.1 list——列表
a = []
len()
a[-1]
a.append()              # 追加在末尾
a.insert(1,'b')         # 在指定位置插入
a.pop(i)                # 删除位置i处的元素
a[1] = xxx              # 元素替换可直接赋值

# 1.3.2 tuple——元组
"可变的“tuple：
t = ('a', 'b', ['A', 'B'])
t[2][0] = A
t[2][1] = B
# 变的不是tuple的元素，而是list的元素（tuple指向不变）

-------------------------------------------------------------

# 1.4 Conditional judgement

# 1.4.1 if
age = 3
if age >= 18:
    print('adult')
elif age>=6:
    print('teenager')
else:
    print('kid')

# 1.4.2 input——int():
s = input('Please input your birth: ')
birth = int(s)
if birth < 2000:
    print('00前')
else:
    print('00后')








